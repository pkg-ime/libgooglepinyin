#!/usr/bin/make -f
LDFLAGS+=-Wl,--as-needed
%:
	dh $@ --with python2
override_dh_auto_configure:
	dh_auto_configure -- -DENABLE_STATIC=True
override_dh_strip:
	dh_strip --dbg-package=libgooglepinyin0-dbg
